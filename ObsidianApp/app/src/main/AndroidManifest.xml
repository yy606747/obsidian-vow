<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 麦克风权限（语音唤醒/通话用） -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- WebRTC 需要修改音频设置 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 保持屏幕常亮（闹铃/监控提醒不错过） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 前台服务权限（后台保持 WebSocket 连接） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- 闹钟、监控与哨兵提醒的锁屏全屏提示 -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 通知权限（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 电池优化白名单引导 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 精确闹钟（用于 Service 被杀后自复活） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- 把用户在对话中设置的闹钟交给系统时钟应用 -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- 网络变化监听（用于断线后自动重连） -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- GPS 定位权限（高德地图位置上报） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- 使用情况访问权限（活动日志采集前台应用） -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- 蓝牙权限（BLE 玩具控制） -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- ══════ 体感/体征/社交脉搏相关新增权限 ══════ -->

    <!-- Health Connect：心率/睡眠/步数/SpO2/压力值（小米运动健康写入） -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />

    <!-- 查询其它 App 的包信息（通知监听解析包名时用到） -->
    <queries>
        <package android:name="androidx.health.connect" />
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_ALARM" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DISMISS_ALARM" />
        </intent>
    </queries>

    <application
        android:name=".ObsidianApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ObsidianVow"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 地址选择页 -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Health Connect 权限解释入口（从 HC App 跳回来） -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- WebView 聊天页 -->
        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden"
            android:screenOrientation="portrait" />

        <!-- 后台推送服务（前台服务 + WebSocket 长连接） -->
        <service
            android:name=".ObsidianPushService"
            android:exported="false"
            android:foregroundServiceType="dataSync|location|mediaPlayback" />

        <!-- 主控模式前台服务（BLE 广播不受后台节流 + 通知栏紧急停止） -->
        <service
            android:name=".DomForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Muse 潮汐前台服务（BLE 广播 + TTL 自动停止） -->
        <service
            android:name=".MuseForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <receiver
            android:name=".MuseStopReceiver"
            android:exported="false" />

        <!-- 智能戒指 BLE 执行器（接收后端 touch 请求并写入 GATT） -->
        <service
            android:name=".SmartRingService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- 移动端一次性截图前台服务（MediaProjection） -->
        <service
            android:name=".MobileScreenCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- 截图确认页（轻量，半透明对话框） -->
        <activity
            android:name=".ScreenCaptureRequestActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- 闹钟、监控与哨兵提醒：仅系统事件使用，普通 AI 消息不会主动亮屏。 -->
        <activity
            android:name=".SystemAlertActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Android 14+ 必需：HC App 通过 VIEW_PERMISSION_USAGE 发现支持健康权限的 App。
             没有这个 activity-alias，HC 在 Android 14+ 上会直接跳过我们的包，
             即使授权成功也不会出现在"已连接的应用"列表里。 -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".LauncherActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <!-- 社交脉搏：监听微信/QQ 通知事件（只读元数据，不读正文） -->
        <service
            android:name=".SocialPulseService"
            android:exported="true"
            android:label="Obsidian Vow 社交脉搏"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
